# Claude Code 프로젝트 지침

이 저장소에서 작업을 시작할 때 다음 문서를 순서대로 **처음부터 끝까지** 읽는다.

1. `AGENTS.md`
2. `PROJECT_CONTEXT.md`
3. `CLAUDE_CODE_HANDOFF.md`
4. 현재 작업과 관련된 `docs/` 문서와 YAML 설정

`AGENTS.md`의 정책은 Claude Code에도 그대로 적용된다. 특히 다음 항목은 사용자 승인
없이 변경하지 않는다.

- 클래스는 `grasp_region=0`, `functional_region=1`이다.
- UMD `contain`의 통합 라벨은 `ignore=255`로 보존한다.
- 컵 몸통을 `functional_region`으로 바꾸지 않는다.
- 머그 손잡이와 몸통은 같은 `grasp_region`이어도 별도 인스턴스로 유지한다.
- 자체 데이터 분할은 `mug_01/mug_02=train`, `mug_03=validation`,
  `mug_04=test 예약`이다.
- 원본 데이터와 사람이 승인한 마스크를 덮어쓰지 않는다.
- 실제 값이나 결과를 확인하지 않고 성공·성능 수치를 만들지 않는다.

코드 주석, docstring, 오류 메시지, CLI 도움말, 문서와 결과 설명은 원칙적으로
한국어로 작성한다. 호환성이 필요한 함수명, 변수명, manifest 키와 고정 라벨은 영문을
유지한다.

## 현재 작업 시작점

자체 머그 140장과 배경 20장의 승인·YOLO 변환·서버 업로드·A/D 학습 설정 dry-run까지
끝났다. **실제 자체 데이터 미세조정은 아직 시작하지 않았다.** 다음 작업은
`CLAUDE_CODE_HANDOFF.md`의 순서를 따라 서버에서 A/D 1 epoch smoke test를 수행하고,
사용자에게 결과를 보고한 뒤 전체 학습을 시작하는 것이다.

서버 작업 규칙:

- SSH 접속 정보 중 비밀번호는 코드, 문서, 로그에 저장하지 않는다.
- 기존 tmux 세션을 사용한다.
- 지정 conda 환경을 활성화한다.
- `sudo`와 서버 자체 업데이트를 사용하지 않는다.
- 데이터와 큰 결과는 `/DATA` 또는 `/data2`에 둔다.
- 전체 학습을 시작한 뒤 초반 2~3 epoch가 정상 진행되는지만 확인하고 tmux에서
  분리한다. 학습을 계속 폴링하지 말고 사용자가 완료를 알릴 때까지 기다린다.

현재 폴더는 Git 저장소로 인식되지 않는다. `git reset`, `git checkout` 같은 복원
명령을 사용하지 말고 기존 파일과 백업을 보존한다.

